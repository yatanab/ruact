export const URLs = {
  root: "/",
  public: "/public",
  private: "/private",
  signin: "/signin",
  signup: "/signup",
  notification: "/notification",
  chart: "/chart",
  apiTest: "/api-test",
  cardList: "/card-List",
}

export type URLsKeys = keyof typeof URLs;